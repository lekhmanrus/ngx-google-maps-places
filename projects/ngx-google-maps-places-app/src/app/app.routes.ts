import { Routes } from '@angular/router';

/**
 * Defines the application's routing configuration.
 * This array of routes is used by the Angular Router to map URLs to components.
 */
export const routes: Routes = [ ];
